import { services } from "@/lib/services";
import Reveal from "./Reveal";
import SectionHeading from "./SectionHeading";

export default function Services() {
  return <section id="services" className="section-space"><div className="container-page"><Reveal><SectionHeading eyebrow="Ways to work together" title="Editorial services shaped around the work you are trying to create." intro="Every engagement begins with the work itself: what it needs, where it is going, and what will make it resonate." /></Reveal><div className="mt-16 border-t hairline">{services.map((service) => <Reveal key={service.name}><article className="group grid gap-6 border-b hairline py-10 transition-colors hover:bg-linen/60 md:grid-cols-[.12fr_.58fr_.3fr] md:px-5 lg:py-12"><span className="eyebrow text-terracotta">{service.number}</span><div><h3 className="font-serif text-3xl text-espresso sm:text-4xl">{service.name}</h3><p className="mt-5 max-w-2xl leading-7 text-espresso/70">{service.description}</p><div className="mt-6 grid gap-5 text-sm sm:grid-cols-2"><p><span className="mb-1 block text-[.58rem] font-semibold uppercase tracking-[.16em] text-terracotta">Ideal for</span>{service.ideal}</p><p><span className="mb-1 block text-[.58rem] font-semibold uppercase tracking-[.16em] text-terracotta">Includes</span>{service.deliverables}</p></div></div><div className="flex items-start justify-between border-t border-stone pt-5 md:block md:border-l md:border-t-0 md:pl-8 md:pt-0"><span className="eyebrow text-espresso/40">Investment</span><p className="mt-0 font-serif text-xl text-espresso md:mt-4">{service.investment}</p></div></article></Reveal>)}</div></div></section>;
}
